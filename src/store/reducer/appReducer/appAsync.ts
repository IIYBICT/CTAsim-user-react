import { createAsyncThunk } from "@reduxjs/toolkit";

// export const projectListAsync = createAsyncThunk(
//   "projectListAsync",
//   async () => {
//     const res = await GetProjectList();
//     return new Promise<GetProjectListRes>((resolve, reject) => {
//       resolve(res.data);
//     });
//   }
// );
